import { SlideDemo } from '../../src/components/slides/reveal-slide';

export default function SlideViewerPage(props: Record<never, unknown>) {
  return (
    <div>
      <SlideDemo />
    </div>
  );
}
